Environment and build files

/node_modules
/dist
/build

IDE and OS files

.vscode/
.DS_Store
\*.swp

Deployment scripts (keep the scripts themselves, but exclude logs/credentials)

/deploy/\*.log

Sensitive API keys should be stored in environment variables, not in this repo.

If using a .env file:

.env
school
school.pub
_.pem
_.key
deploy/\*.log
deploy/temp/
